global:
  scrape_interval: 15s 
remote_write:
  - url: https://prometheus-prod-52-prod-ap-southeast-2.grafana.net/api/prom/push
    basic_auth:
      username: ${GRAFANA_USERNAME}
      password: ${GRAFANA_PASSWORD}

scrape_configs:
  - job_name: "fundraise_api"
    scheme: https
    static_configs:
      - targets: ["krzpztrk-api-model-fundraise-v3.hf.space"]